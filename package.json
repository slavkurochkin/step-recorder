{
  "name": "step-recorder",
  "version": "1.0.0",
  "description": "Chrome extension for recording user interactions and generating test cases",
  "type": "module",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "@langchain/openai": "^0.3.0",
    "langchain": "^0.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
